{
  "scripts": {
    "build": "npx tsc && npx node-gyp rebuild",
    "start": "node dist/server/app.js",
    "test": "jest"
  },
  "dependencies": { "fastify": "^4.0.0", "node-addon-api": "^7.0.0" },
  "devDependencies": { "typescript": "^5.0.0", "jest": "^29.0.0", "@types/node": "^20.0.0" }
}
